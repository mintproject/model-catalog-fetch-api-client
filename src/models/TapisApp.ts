// tslint:disable
/**
 * TapisApp Model
 * This model represents a Tapis Application definition.
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { exists } from '../runtime';

/**
 * Enumeration of input modes for arguments
 * @export
 * @enum {string}
 */
export enum ArgumentInputModeEnum {
  REQUIRED = 'REQUIRED',
  FIXED = 'FIXED',
  INCLUDE_ON_DEMAND = 'INCLUDE_ON_DEMAND',
  INCLUDE_BY_DEFAULT = 'INCLUDE_BY_DEFAULT',
}

/**
 * Command line argument for job execution
 * @export
 * @interface Argument
 */
export interface Argument {
  /**
   * Argument name
   * @type {string}
   * @memberof Argument
   */
  name: string;
  /**
   * Argument description
   * @type {string}
   * @memberof Argument
   */
  description?: string;
  /**
   * Input mode for the argument
   * @type {ArgumentInputModeEnum}
   * @memberof Argument
   */
  inputMode?: ArgumentInputModeEnum;
  /**
   * Argument value
   * @type {string}
   * @memberof Argument
   */
  value?: string;
  /**
   * Order of the argument in the command line
   * @type {number}
   * @memberof Argument
   */
  order?: number;
}

/**
 * Environment variable for job execution
 * @export
 * @interface EnvironmentVariable
 */
export interface EnvironmentVariable {
  /**
   * Environment variable key
   * @type {string}
   * @memberof EnvironmentVariable
   */
  key: string;
  /**
   * Environment variable value
   * @type {string}
   * @memberof EnvironmentVariable
   */
  value: string;
}

/**
 * Filter for archiving job files
 * @export
 * @interface ArchiveFilter
 */
export interface ArchiveFilter {
  /**
   * Files to include in archive
   * @type {Array<string>}
   * @memberof ArchiveFilter
   */
  includes?: Array<string>;
  /**
   * Files to exclude from archive
   * @type {Array<string>}
   * @memberof ArchiveFilter
   */
  excludes?: Array<string>;
  /**
   * Whether to include launch files in archive
   * @type {boolean}
   * @memberof ArchiveFilter
   */
  includeLaunchFiles?: boolean;
}

/**
 * Configuration for job logging
 * @export
 * @interface LogConfig
 */
export interface LogConfig {
  /**
   * Filename for stdout
   * @type {string}
   * @memberof LogConfig
   */
  stdoutFilename?: string;
  /**
   * Filename for stderr
   * @type {string}
   * @memberof LogConfig
   */
  stderrFilename?: string;
}

/**
 * Set of parameters for job execution
 * @export
 * @interface ParameterSet
 */
export interface ParameterSet {
  /**
   * Application arguments
   * @type {Array<Argument>}
   * @memberof ParameterSet
   */
  appArgs?: Array<Argument>;
  /**
   * Container arguments
   * @type {Array<Argument>}
   * @memberof ParameterSet
   */
  containerArgs?: Array<Argument>;
  /**
   * Scheduler options
   * @type {Array<Argument>}
   * @memberof ParameterSet
   */
  schedulerOptions?: Array<Argument>;
  /**
   * Environment variables
   * @type {Array<EnvironmentVariable>}
   * @memberof ParameterSet
   */
  envVariables?: Array<EnvironmentVariable>;
  /**
   * Archive filter configuration
   * @type {ArchiveFilter}
   * @memberof ParameterSet
   */
  archiveFilter?: ArchiveFilter;
  /**
   * Log configuration
   * @type {LogConfig}
   * @memberof ParameterSet
   */
  logConfig?: LogConfig;
}

/**
 * File input definition for jobs
 * @export
 * @interface FileInput
 */
export interface FileInput {
  // File input properties would be defined here
}

/**
 * Job attributes for Tapis applications
 * @export
 * @interface JobAttributes
 */
export interface JobAttributes {
  /**
   * Job description
   * @type {string}
   * @memberof JobAttributes
   */
  description?: string;
  /**
   * Whether to use dynamic execution system
   * @type {boolean}
   * @memberof JobAttributes
   */
  dynamicExecSystem?: boolean;
  /**
   * Execution system constraints
   * @type {object}
   * @memberof JobAttributes
   */
  execSystemConstraints?: object;
  /**
   * Execution system identifier
   * @type {string}
   * @memberof JobAttributes
   */
  execSystemId: string;
  /**
   * Execution directory on the system
   * @type {string}
   * @memberof JobAttributes
   */
  execSystemExecDir: string;
  /**
   * Input directory on the system
   * @type {string}
   * @memberof JobAttributes
   */
  execSystemInputDir: string;
  /**
   * Output directory on the system
   * @type {string}
   * @memberof JobAttributes
   */
  execSystemOutputDir: string;
  /**
   * DTN input directory
   * @type {string}
   * @memberof JobAttributes
   */
  dtnSystemInputDir?: string;
  /**
   * DTN output directory
   * @type {string}
   * @memberof JobAttributes
   */
  dtnSystemOutputDir?: string;
  /**
   * Logical queue for execution
   * @type {string}
   * @memberof JobAttributes
   */
  execSystemLogicalQueue?: string;
  /**
   * Archive system identifier
   * @type {string}
   * @memberof JobAttributes
   */
  archiveSystemId?: string;
  /**
   * Archive directory on the system
   * @type {string}
   * @memberof JobAttributes
   */
  archiveSystemDir?: string;
  /**
   * Whether to archive on application error
   * @type {boolean}
   * @memberof JobAttributes
   */
  archiveOnAppError?: boolean;
  /**
   * Whether the job uses MPI
   * @type {boolean}
   * @memberof JobAttributes
   */
  isMpi?: boolean;
  /**
   * MPI command
   * @type {string}
   * @memberof JobAttributes
   */
  mpiCmd?: string;
  /**
   * Command prefix
   * @type {string}
   * @memberof JobAttributes
   */
  cmdPrefix?: string;
  /**
   * Parameter set for the job
   * @type {ParameterSet}
   * @memberof JobAttributes
   */
  parameterSet?: ParameterSet;
  /**
   * File inputs for the job
   * @type {Array<FileInput>}
   * @memberof JobAttributes
   */
  fileInputs?: Array<FileInput>;
  /**
   * File input arrays
   * @type {Array<any>}
   * @memberof JobAttributes
   */
  fileInputArrays?: Array<any>;
  /**
   * Number of nodes to use
   * @type {number}
   * @memberof JobAttributes
   */
  nodeCount?: number;
  /**
   * Cores per node
   * @type {number}
   * @memberof JobAttributes
   */
  coresPerNode?: number;
  /**
   * Memory in MB
   * @type {number}
   * @memberof JobAttributes
   */
  memoryMB?: number;
  /**
   * Maximum runtime in minutes
   * @type {number}
   * @memberof JobAttributes
   */
  maxMinutes?: number;
  /**
   * Job subscriptions
   * @type {Array<any>}
   * @memberof JobAttributes
   */
  subscriptions?: Array<any>;
  /**
   * Job tags
   * @type {Array<string>}
   * @memberof JobAttributes
   */
  tags?: Array<string>;
}

/**
 * @export
 * @interface TapisApp
 */
export interface TapisApp {
  /**
   * The tenant ID
   * @type {string}
   * @memberof TapisApp
   */
  tenant?: string;
  /**
   * The application ID
   * @type {string}
   * @memberof TapisApp
   */
  id?: string;
  /**
   * The application version
   * @type {string}
   * @memberof TapisApp
   */
  version?: string;
  /**
   * The application description
   * @type {string}
   * @memberof TapisApp
   */
  description?: string;
  /**
   * The application owner
   * @type {string}
   * @memberof TapisApp
   */
  owner?: string;
  /**
   * Whether the application is enabled
   * @type {boolean}
   * @memberof TapisApp
   */
  enabled?: boolean;
  /**
   * Whether the version is enabled
   * @type {boolean}
   * @memberof TapisApp
   */
  versionEnabled?: boolean;
  /**
   * Whether the application is locked
   * @type {boolean}
   * @memberof TapisApp
   */
  locked?: boolean;
  /**
   * Whether the application is public
   * @type {boolean}
   * @memberof TapisApp
   */
  isPublic?: boolean;
  /**
   * Users the application is shared with
   * @type {Array<string>}
   * @memberof TapisApp
   */
  sharedWithUsers?: Array<string>;
  /**
   * The runtime environment
   * @type {string}
   * @memberof TapisApp
   */
  runtime?: string;
  /**
   * The runtime version
   * @type {string}
   * @memberof TapisApp
   */
  runtimeVersion?: string;
  /**
   * Runtime options
   * @type {Array<string>}
   * @memberof TapisApp
   */
  runtimeOptions?: Array<string>;
  /**
   * The container image
   * @type {string}
   * @memberof TapisApp
   */
  containerImage?: string;
  /**
   * The job type
   * @type {string}
   * @memberof TapisApp
   */
  jobType?: string;
  /**
   * Maximum number of jobs
   * @type {number}
   * @memberof TapisApp
   */
  maxJobs?: number;
  /**
   * Maximum jobs per user
   * @type {number}
   * @memberof TapisApp
   */
  maxJobsPerUser?: number;
  /**
   * Whether to use strict file inputs
   * @type {boolean}
   * @memberof TapisApp
   */
  strictFileInputs?: boolean;
  /**
   * Job attributes
   * @type {JobAttributes}
   * @memberof TapisApp
   */
  jobAttributes?: JobAttributes;
  /**
   * Application tags
   * @type {Array<string>}
   * @memberof TapisApp
   */
  tags?: Array<string>;
  /**
   * Application notes
   * @type {object}
   * @memberof TapisApp
   */
  notes?: object;
  /**
   * Shared application context
   * @type {string}
   * @memberof TapisApp
   */
  sharedAppCtx?: string;
  /**
   * Application UUID
   * @type {string}
   * @memberof TapisApp
   */
  uuid?: string;
  /**
   * Whether the application is deleted
   * @type {boolean}
   * @memberof TapisApp
   */
  deleted?: boolean;
  /**
   * Creation timestamp
   * @type {string}
   * @memberof TapisApp
   */
  created?: string;
  /**
   * Last update timestamp
   * @type {string}
   * @memberof TapisApp
   */
  updated?: string;
}

export function TapisAppFromJSON(json: any): TapisApp {
  return TapisAppFromJSONTyped(json, false);
}

export function TapisAppFromJSONTyped(
  json: any,
  ignoreDiscriminator: boolean,
): TapisApp {
  if (json === undefined || json === null) {
    return json;
  }
  return {
    tenant: !exists(json, 'tenant') ? undefined : json['tenant'],
    id: !exists(json, 'id') ? undefined : json['id'],
    version: !exists(json, 'version') ? undefined : json['version'],
    description: !exists(json, 'description') ? undefined : json['description'],
    owner: !exists(json, 'owner') ? undefined : json['owner'],
    enabled: !exists(json, 'enabled') ? undefined : json['enabled'],
    versionEnabled: !exists(json, 'versionEnabled')
      ? undefined
      : json['versionEnabled'],
    locked: !exists(json, 'locked') ? undefined : json['locked'],
    isPublic: !exists(json, 'isPublic') ? undefined : json['isPublic'],
    sharedWithUsers: !exists(json, 'sharedWithUsers')
      ? undefined
      : json['sharedWithUsers'],
    runtime: !exists(json, 'runtime') ? undefined : json['runtime'],
    runtimeVersion: !exists(json, 'runtimeVersion')
      ? undefined
      : json['runtimeVersion'],
    runtimeOptions: !exists(json, 'runtimeOptions')
      ? undefined
      : json['runtimeOptions'],
    containerImage: !exists(json, 'containerImage')
      ? undefined
      : json['containerImage'],
    jobType: !exists(json, 'jobType') ? undefined : json['jobType'],
    maxJobs: !exists(json, 'maxJobs') ? undefined : json['maxJobs'],
    maxJobsPerUser: !exists(json, 'maxJobsPerUser')
      ? undefined
      : json['maxJobsPerUser'],
    strictFileInputs: !exists(json, 'strictFileInputs')
      ? undefined
      : json['strictFileInputs'],
    jobAttributes: !exists(json, 'jobAttributes')
      ? undefined
      : json['jobAttributes'],
    tags: !exists(json, 'tags') ? undefined : json['tags'],
    notes: !exists(json, 'notes') ? undefined : json['notes'],
    sharedAppCtx: !exists(json, 'sharedAppCtx')
      ? undefined
      : json['sharedAppCtx'],
    uuid: !exists(json, 'uuid') ? undefined : json['uuid'],
    deleted: !exists(json, 'deleted') ? undefined : json['deleted'],
    created: !exists(json, 'created') ? undefined : json['created'],
    updated: !exists(json, 'updated') ? undefined : json['updated'],
  };
}

export function TapisAppToJSON(value?: TapisApp): any {
  if (value === undefined) {
    return undefined;
  }
  if (value === null) {
    return null;
  }
  return {
    tenant: value.tenant,
    id: value.id,
    version: value.version,
    description: value.description,
    owner: value.owner,
    enabled: value.enabled,
    versionEnabled: value.versionEnabled,
    locked: value.locked,
    isPublic: value.isPublic,
    sharedWithUsers: value.sharedWithUsers,
    runtime: value.runtime,
    runtimeVersion: value.runtimeVersion,
    runtimeOptions: value.runtimeOptions,
    containerImage: value.containerImage,
    jobType: value.jobType,
    maxJobs: value.maxJobs,
    maxJobsPerUser: value.maxJobsPerUser,
    strictFileInputs: value.strictFileInputs,
    jobAttributes: value.jobAttributes,
    tags: value.tags,
    notes: value.notes,
    sharedAppCtx: value.sharedAppCtx,
    uuid: value.uuid,
    deleted: value.deleted,
    created: value.created,
    updated: value.updated,
  };
}
